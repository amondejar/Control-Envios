namespace ControlEnvios.Domain.Enums;

/// <summary>Rol con el que un usuario accede a la aplicación tras autenticarse.</summary>
public enum RolUsuario
{
    Proveedor,
    Gestor,
    Produccion,
}
